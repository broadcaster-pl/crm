{
  "name": "streamflow-document-generator",
  "version": "1.0.0",
  "description": "StreamFlow Document Generator",
  "main": "document-generator.js",
  "scripts": {
    "generate": "node document-generator.js",
    "test": "jest --coverage --runInBand --forceExit",
    "test:watch": "jest --watch --runInBand"
  },
  "dependencies": {
    "docx": "^9.5.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage"
  }
}
